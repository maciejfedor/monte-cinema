<%= form_with(url: screening_reservations_path, method: "post") do |f|%>
  <%= f.select :seats, options_for_select(@screening.hall.make_seats), {}, {multiple: true, size: 10} %>
  <%= f.submit 'Book a reservation' %>
<% end %>
