<nav class="navbar navbar-expand-lg bg-light">
  <div class="container-fluid">
    <%= link_to "MONTE CINEMA", root_path, class: "navbar-brand  fw-bolder text-body ", id: "logo" %>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav me-auto mb-2 mb-lg-0">
        <% if user_signed_in? %>
          <li class="nav-item dropdown">
            <a class="nav-link dropdown-toggle text-success fw-bold" href="#" id="navbarDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">
              Profile [<%= current_user.email %>]
            </a>
            <ul class="dropdown-menu" aria-labelledby="navbarDropdown">
              <li class="nav-item">
                <% if !current_user.user?%>
                  <%= link_to "All reservations", reservations_path(), class: "nav-link text-body" %>
                </li>
              <%else%>
                <li class="nav-item">
                  <%= link_to "My reservations", user_path(), class: "nav-link text-body" %>
                </li>
              <%end %>
            </ul>
          </li>
          <% if !current_user.user?%>
            <li class="nav-item">
              <%= link_to 'Movies',  movies_path, class: "nav-link fw-bold text-success " %>
            </li>
          <% end %>
          <li class="nav-item">
            <%= link_to 'Logout', destroy_user_session_path, class: "nav-link fw-bold text-body ",  data: { "turbo-method": :delete} %>
          </li>
        <% else %>
          <li class="nav-item">
            <%= link_to 'Log in',  new_user_session_path, class: "nav-link fw-bold text-success" %>
          </li>
          <li class="nav-item">
            <%= link_to 'Sign up',  new_user_registration_path, class: "nav-link fw-bold text-body" %>
          </li>
        <% end %>
        <li class="nav-item">
          <%= link_to 'Screenings',  screenings_path, class: "nav-link fw-bold text-body" %>
        </li>
      </li>
    </ul>
  </li>
</ul>
</div>
</div>
</nav>
